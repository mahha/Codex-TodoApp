# レビュー結果: Issue #4

## レビュー対象
- Issue: #4
- ブランチ: main
- 比較対象: main ブランチ
- レビュー日時: 2026-01-21 01:26

## 全体所感
- モックデータ導入と一覧描画の流れは Issue の意図に沿っており、最小のUIとテスト追加も実施されています。
- 一方で、操作ボタンに該当するUIが含まれており、Issueの「本フェーズ対象外」との整合性に注意が必要です。

## 指摘事項（修正必須）
- [ ] 操作ボタン（+ Add Task）が本フェーズの範囲外
  - 該当ファイル: app/routes/home.tsx
  - 該当箇所: app/routes/home.tsx:46
  - 理由: Issueでは「操作ボタンや編集/削除は本フェーズ対象外」と明記されており、UI上の追加ボタンは仕様逸脱の可能性があります。
  - 修正提案: Figmaに明確に存在する場合は Issue に追記して許容範囲を明確化、そうでなければボタンを削除してください。

## 改善提案（任意）
- [ ] 空データ時の表示とテストを追加
  - 期待される効果: 一覧が空の場合のUX/表示崩れを早期に検知できます。
  - 実装の方向性（概要レベル）: `mockTodos` を空配列にした場合の表示文言を決め、`Home` の描画テストを追加します。

## 質問・確認事項
- `dev` スクリプトに `--host` を追加した意図はありますか？（Issueには記載がなく、開発サーバ公開のリスクが増えるため）
- `eslint.config.js` の追加は本Issueのスコープに含める前提でしょうか？

## 良い点
- `loader` でモックデータを返し、Cloudflare env 依存を排除している点はIssue要件に一致しています。
- `Home` の描画テストが追加されており、最低限のUI保証ができています。
- `<main>`, `<ul>/<li>` のセマンティクスが保たれており、構造が読みやすいです。
